extends page.pug

block content
        div(class= ["container"])
                div(class="jumbotron")
                        h1(class="text-center")= title
                        -var postDate = new Date(Date.parse(date))
                        h4= postDate.toDateString()
                block blogcontent
                include pagination.pug
